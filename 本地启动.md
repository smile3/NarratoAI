# NarratoAI 本地启动说明

本文档记录本地 Docker 启动、普通 Python 启动，以及常见启动失败的排查方式。

## 本次 Docker 启动失败原因

报错核心信息：

```text
error mounting ".../NarratoAI/config.toml" to rootfs at "/NarratoAI/config.toml": not a directory: Are you trying to mount a directory onto a file (or vice-versa)?
```

根因是项目根目录下的 `config.toml` 被创建成了目录：

```bash
stat config.toml
```

如果看到它是 `Directory`，而不是普通文件，Docker 会把宿主机的目录挂载到容器内 `/NarratoAI/config.toml` 这个文件路径，于是容器在启动前就失败。

这个目录通常是因为之前执行过 `docker compose up` 时，宿主机上没有 `config.toml` 文件，旧写法的 bind mount 自动创建了一个同名目录。

修复方式：如果 `config.toml` 是空目录，删除目录并从模板复制配置文件。

```bash
rmdir config.toml
cp config.example.toml config.toml
```

如果 `config.toml` 目录不是空的，先检查并备份里面的内容，再删除该目录。

## Docker 启动

### 1. 准备配置文件

```bash
cp config.example.toml config.toml
```

打开 `config.toml`，至少配置你要使用的大模型或语音服务 API Key。常见字段包括：

- `app.vision_openai_api_key`
- `app.text_openai_api_key`
- `app.vision_openai_base_url`
- `app.text_openai_base_url`
- `ui.tts_engine`

如果只是先验证 WebUI 能否启动，可以先保留空 Key，应用页面能打开，但调用生成、分析、配音等功能时会要求配置对应密钥。

### 2. 启动服务

推荐使用 Docker Compose V2：

```bash
docker compose up -d --build
```

也可以使用项目脚本：

```bash
./docker-deploy.sh --build
```

启动完成后访问：

```text
http://localhost:8501
```

### 3. 查看状态和日志

```bash
docker compose ps
docker compose logs -f narratoai-webui
```

停止服务：

```bash
docker compose down
```

重启服务：

```bash
docker compose restart
```

## Makefile 启动

项目也提供了常用 Make 命令：

```bash
make config
make build
make up
make logs
make down
```

默认使用 `docker compose`。如果你的环境只安装了旧版 `docker-compose`，可以这样覆盖：

```bash
make COMPOSE=docker-compose up
```

## Python 本地启动

如果不使用 Docker，可以直接用 Python 运行。

### 1. 创建虚拟环境

```bash
python3.12 -m venv .venv
source .venv/bin/activate
```

### 2. 安装依赖

```bash
python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

macOS 本地运行还需要安装视频处理依赖：

```bash
brew install ffmpeg imagemagick
```

### 3. 准备配置文件

```bash
cp config.example.toml config.toml
```

按需编辑 `config.toml` 中的 API Key、模型名、Base URL 和 TTS 引擎。

### 4. 启动 WebUI

```bash
streamlit run webui.py --server.maxUploadSize=2048
```

访问：

```text
http://localhost:8501
```

## 常见问题

### `config.toml` 变成目录

检查：

```bash
stat config.toml
```

修复空目录：

```bash
rmdir config.toml
cp config.example.toml config.toml
```

之后重新启动：

```bash
docker compose up -d
```

### 端口 8501 被占用

检查占用进程：

```bash
lsof -i :8501
```

如果只是旧容器还在运行，可以停止：

```bash
docker compose down
```

也可以临时改 `docker-compose.yml` 的端口映射，例如：

```bash
NARRATOAI_WEBUI_PORT=8502 docker compose up -d
```

然后访问 `http://localhost:8502`。不设置 `NARRATOAI_WEBUI_PORT` 时默认仍使用 `8501`。

### 镜像构建后仍使用旧代码

强制重新构建：

```bash
docker compose build --no-cache
docker compose up -d
```

或使用部署脚本：

```bash
./docker-deploy.sh --no-cache
```

### 查看容器内文件

```bash
docker compose exec narratoai-webui bash
ls -la /NarratoAI
```

确认容器内 `/NarratoAI/config.toml` 是文件：

```bash
docker compose exec narratoai-webui test -f /NarratoAI/config.toml
```

## 建议启动顺序

第一次启动建议按下面顺序执行：

```bash
make config
docker compose up -d --build
docker compose ps
docker compose logs -f narratoai-webui
```

后续日常启动只需要：

```bash
docker compose up -d
```
